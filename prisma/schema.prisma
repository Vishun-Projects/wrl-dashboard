// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model CallsCache {
  id            String   @id
  vtrnno        String
  customer_name String
  engineer_name String
  branch_name   String
  office_id     String
  status_label  String
  is_major      Boolean
  visit_count   Int
  part_count    Int
  logged_at     DateTime
  started_at    DateTime?
  resolved_at   DateTime?
  synced_at     DateTime @default(now())
  serial_number String?
  manual_job_no String?
  vcomplaint    String?
  vlocation     String?
  audit_flag    String?
  solve_remarks String?
  cancel_reason String?
  approved_at   DateTime?
  updated_at    DateTime @default(now())

  @@map("calls_cache")
}

model CallComment {
  id          Int      @id @default(autoincrement())
  call_id     String
  content     String
  created_at  DateTime @default(now())
  author_id   String?
  author_name String?

  @@map("call_comments")
}

model AppUser {
  id         String   @id @default(uuid())
  email      String   @unique
  name       String
  role       String   // "branch_manager", "hod"
  office_ids String[] @default([]) // Array of ncode values from CRM mstoffice
  created_at DateTime @default(now())

  @@map("app_users")
}

model SyncStatus {
  id           Int      @id @default(1)
  is_running   Boolean  @default(false)
  progress     Int      @default(0)
  current_step String?
  total_chunks Int      @default(0)
  done_chunks  Int      @default(0)
  abort_requested Boolean  @default(false)
  last_updated DateTime @default(now())

  @@map("sync_status")
}

model CachedVisit {
  id               String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  call_id          String
  office_id        String
  person_contacted String?
  visit_date       DateTime?
  remark           String?
  duration         Int?
  expense          Decimal? @db.Decimal
  created_at       DateTime @default(now())

  @@index([call_id, office_id], name: "idx_cached_visits_call")
  @@map("cached_visits")
}

model CachedPart {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  call_id    String
  office_id  String
  part_name  String?
  part_code  String?
  quantity   Int?
  created_at DateTime @default(now())

  @@index([call_id, office_id], name: "idx_cached_parts_call")
  @@map("cached_parts")
}
